def sum_using_for_loop(numbers):
    total = 0
    for num in numbers:
        total += num
    return total

# Sample Input
numbers = [1, 2, 3, 4, 5]

# Calculating sum using for loop
sum_for = sum_using_for_loop(numbers)

# Output the result
print("Sum of elements using for loop:", sum_for)
