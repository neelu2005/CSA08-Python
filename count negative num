def count_negative_numbers(nums):
    count = 0
    for num in nums:
        if num < 0:
            count += 1
    return count

# Example usage:
numbers = [-2, 3, 0, -5, 7, -1, 9, -4]
print("Number of negative numbers:", count_negative_numbers(numbers))
